# Branch: claude/up-007

Tracking branch for UP-007 — attach an FQDN to a nameless instance in
place: prove the zone, issue the certificate, re-render configuration,
and report the clone URLs that changed, without rebuilding the instance
or losing repositories, history, pull requests, review comments, CI
history, secrets, or the SSH host key.

See `docs/functional-requirements.md` § UP and `docs/status.json`.
