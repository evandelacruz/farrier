# Branch: claude/key-003

Tracking branch for KEY-001, KEY-002, and KEY-003 — the keystore driver
interface plus its two shipped drivers (`file`, `command`) and the
never-leaks guarantee on resolved key material.

See `docs/functional-requirements.md` § KEY and `docs/status.json`.
