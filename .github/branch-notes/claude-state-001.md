# Branch: claude/state-001

Tracking branch for STATE-001 — git data exportable as a mirrorable set of
remotes, so backup and the operator's own replication tooling can point at
it without reaching into Forgejo's storage directly.

See `docs/functional-requirements.md` § STATE and `docs/status.json`.
