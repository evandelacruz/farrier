# Branch: claude/dns-001-dns-002

Tracking branch for DNS-001 and DNS-002 — the DNS driver interface plus
its two shipped drivers: `cloudflare` (Cloudflare API) and `rfc2136`
(nsupdate/TSIG).

See `docs/functional-requirements.md` § DNS and `docs/status.json`.
