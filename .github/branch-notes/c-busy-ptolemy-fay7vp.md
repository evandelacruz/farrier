# Branch: c/busy-ptolemy-fay7vp

Tracking branch for FORGE-001 — render Forgejo's `app.ini` fully from the
bundle manifest and resolved key material, so the install wizard is
pre-answered and never presented.

See `docs/functional-requirements.md` § FORGE and `docs/status.json`.
