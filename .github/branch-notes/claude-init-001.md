# Branch: claude/init-001

Tracking branch for the remaining slice of INIT-001 — `init` taking the
project folder it serves and defaulting the bundle to `.farrier/` inside
that folder, with the location still overridable for a bundle whose
instance serves several projects.

See `docs/functional-requirements.md` § INIT and `docs/status.json`.
