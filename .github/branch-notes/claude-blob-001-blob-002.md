# Branch: claude/blob-001-blob-002

Tracking branch for BLOB-001 and BLOB-002 — the blob adapter interface plus
its two shipped adapters: `local` (filesystem, streaming) and `s3` (any
S3-compatible endpoint, streaming).

See `docs/functional-requirements.md` § BLOB and `docs/status.json`.
